import { Box } from "@mui/material";

const RafflesRevealTicketSectionLogoTextContainer = ({ children }) => {
  return <Box sx={{ display: "flex", px: 6 }}>{children}</Box>;
};

export default RafflesRevealTicketSectionLogoTextContainer;
