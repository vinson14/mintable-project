import { Box } from "@mui/material";

const RafflesRevealTicketSectionButtonContainer = ({ children }) => {
  return <Box sx={{ display: "flex", flexDirection: "column", justifyContent: "center", width: 230 }}>{children}</Box>;
};

export default RafflesRevealTicketSectionButtonContainer;
